import { codeBlock } from '../../../../../utils'

const path = 'conflicts'

const title = `Конфликты`

const codeBase = `a`

const paragraph = `
${codeBlock(codeBase)}
`

export const conflictsData = {
  path,
  title,
  content: paragraph,
  practice: null,
}
