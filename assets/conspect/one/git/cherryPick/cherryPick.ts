import { codeBlock } from '../../../../../utils'

const path = 'cherryPick'

const title = `Черри-пик`

const codeBase = `a`

const paragraph = `
${codeBlock(codeBase)}
`

export const cherryPickData = {
  path,
  title,
  content: paragraph,
  practice: null,
}
